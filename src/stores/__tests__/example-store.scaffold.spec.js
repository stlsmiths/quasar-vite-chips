import {describe,it} from 'vitest'

describe('example store tests', () => {

  it('store exists', () => {})

  describe('state', () => {
    it('exists', () => {})
    it('has proper initialState', () => {})
  })
  describe('getters', () => {
    it('work and return as expected', () => {})

  })

})
